[project]
name = "wavebank-otakus-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1,<4",
    "langgraph>=1.2,<2",
    "httpx>=0.28",
    "openai>=1.66,<3",
    "cryptography>=42",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
